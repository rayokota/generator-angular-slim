language: node_js
node_js:
  - '0.10'
  - '0.8'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angular-slim' ]; then cd .. && eval "mv $currentfolder generator-angular-slim" && cd generator-angular-slim; fi

